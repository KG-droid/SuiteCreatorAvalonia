namespace SuiteUserPopup.Models
{
    public enum PopupAction
    {
        Continue,
        Skip,
    }
    public enum SuiteAction
    {
        Deployment,
        Removal,
        Rollback,
    }
}
